import React from "react";
import { useNavigate } from 'react-router-dom';

const Work = () => {
    return (
        <div>
            <h1>
                work
            </h1>
        </div>
    );
};

export default Work;